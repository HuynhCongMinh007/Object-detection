--extra-index-url https://download.pytorch.org/whl/cu121

# Khóa chặt phiên bản GPU xịn, cấm tuyệt đối pip tự ý cài bản CPU
torch==2.2.1+cu121
torchvision==0.17.1+cu121

# Các thư viện bổ trợ (Khóa chặt phiên bản để tránh xung đột)
numpy<2
opencv-python<4.10
pillow>=10.0.0

# Note: We intentionally exclude pycocotools to avoid Windows C++ compiler requirements
# COCO JSON parsing is handled natively using Python's built-in json module